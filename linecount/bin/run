#!/bin/sh -e

# for this test the line count can either come from the arguments or
# from an environment variable
if [ -z "$1" ] ; then lines=$LINE_COUNT ; else lines=$1 ; fi
echo line count is $lines
exit 0
